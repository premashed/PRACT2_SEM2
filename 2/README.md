файловый менеджер с использованием библиотеки os
дотупные команды changedir (cd <>), quit (выйти), up (cd ..), makedir(mkdir), deldir(rm -r <>), make(touch <>), write(cat <> >> <>), read(cat <>), del(rm <>), move(mv <> <>), rename(mv <> <>)
